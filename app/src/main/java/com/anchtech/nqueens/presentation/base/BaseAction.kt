package com.anchtech.nqueens.presentation.base

/**
 * Marker for a one-time effect emitted by a ViewModel.
 *
 * Actions are dropped while the screen is off-composition and never replayed.
 */
interface BaseAction
