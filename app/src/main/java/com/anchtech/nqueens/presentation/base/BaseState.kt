package com.anchtech.nqueens.presentation.base

/**
 * Marker for a screen's immutable UI state.
 *
 * Implementations are `data class`es holding everything needed to render the screen,
 * plus the callbacks the screen invokes.
 */
interface BaseState
